import { NavbarHoverDirective } from './navbar-hover.directive';

describe('NavbarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
